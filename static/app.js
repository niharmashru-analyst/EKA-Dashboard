let DATA=[],VAR=[],FILTERED=[],filters={},page='overview',charts=[];
let paretoMode='value',varianceMetric='qty',varianceShop='',varianceSkuSearch='',varianceIssueOnly=false,DATA_COLUMNS=[];
const FILTER_FIELDS=['Type','Store Name','Pareto','NOD Bucket'];
const moneyL=n=>'₹'+(Number(n||0)/1e5).toFixed(2)+' L';
const qty=n=>Math.round(Number(n||0)).toLocaleString('en-IN');
const pct1=n=>Number(n||0).toFixed(1)+'%';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const sum=(a,c)=>a.reduce((x,r)=>x+Number(r[c]||0),0);
const uniq=c=>[...new Set(DATA.map(r=>r[c]).filter(x=>x!==''&&x!=null))].sort((a,b)=>String(a).localeCompare(String(b),undefined,{numeric:true}));
const valueCols=new Set(['total mrp value','l3m avg value','ly value','current month value','mrp','stock value','opening stock value','inward value','tertiary value','closing stock value','calculated closing value','difference value','actual closing value']);
const qtyCols=new Set(['stock','l3m avg qty','ly qty','current month qty','ideal stock','forecast qty','rows','avg','stock qty','opening stock qty','inward qty','tertiary qty','closing stock qty','calculated closing qty','difference qty','skucount','system stock qty','actual closing qty','nod count']);
function fmt(c,v){let k=String(c).toLowerCase();if(k==='nod')return Math.round(Number(v||0)).toLocaleString('en-IN');if(v===''||v==null)return '—';if(valueCols.has(k))return moneyL(v);if(qtyCols.has(k))return qty(v);return v}
function nodClass(v){let n=Number(v||0);return n<15?'nod-red':n<=30?'nod-yellow':'nod-green'}
function nodBucket(v){let n=Number(v||0);if(n<15)return 'NOD <15';if(n<=30)return 'NOD 15–30';if(n<=60)return 'NOD 31–60';return 'NOD >60'}
function normalizePareto(v){let s=String(v??'').trim().toLowerCase().replace(/\s+/g,' ');if(s.includes('top 10'))return 'Top 10';if(s.includes('top 25'))return 'Top 25';if(s.includes('top 50'))return 'Top 50';if(s.includes('top 100'))return 'Top 100';if(s.includes('top 200'))return 'Top 200';if(s.includes('discontinued'))return 'Discontinued';if(s.includes('tail'))return 'Tail';if(s.includes('other'))return 'Others';return String(v??'').trim()||'Others'}
function cell(c,v){let k=String(c).toLowerCase();if(k==='nod')return `<span class="nod-badge ${nodClass(v)}">${fmt(c,v)}</span>`;if(k==='movement check'||k==='live submission')return `<span class="check-badge ${v?'ok':'error'}">${v?'YES':'NO'}</span>`;return esc(fmt(c,v))}
function kpi(t,v,s='',cls=''){return `<div class="kpi ${cls}"><div class="k-title">${esc(t)}</div><div class="k-value">${v}</div><div class="k-sub">${esc(s)}</div></div>`}
function chart(id,opt,clickFn){let e=document.getElementById(id);if(!e)return;try{let old=echarts.getInstanceByDom(e);if(old)old.dispose();let c=echarts.init(e);c.setOption(opt,true);c.resize();if(clickFn)c.on('click',clickFn);charts.push(c)}catch(err){e.innerHTML='<div class="empty">Chart could not render.</div>'}}
const palette=['#2563EB','#0EA5E9','#0F766E','#7C3AED','#D97706','#475569','#14B8A6','#DC2626'];
const base={color:palette,textStyle:{fontFamily:'Manrope',color:'#172033'},tooltip:{trigger:'axis',backgroundColor:'#fff',borderColor:'#D9E1EC',textStyle:{color:'#172033'}},grid:{left:55,right:45,top:70,bottom:65,containLabel:true},axisLabel:{color:'#64748B',fontSize:11},splitLine:{lineStyle:{color:'#E7EDF5'}}};
function apply(){let q=(filters.__q||'').toLowerCase();FILTERED=DATA.filter(r=>FILTER_FIELDS.every(c=>!filters[c]||!filters[c].length||filters[c].includes(String(r[c])))).filter(r=>!q||String(r['Product Name']).toLowerCase().includes(q)||String(r['EAN Code']).toLowerCase().includes(q))}
function triggerLabel(c){let s=filters[c]||[];return !s.length?'All '+c.toLowerCase():s.length<=2?s.join(', '):s.length+' selected'}
function filterBar(){let h='<div class="filters"><div class="filter-head"><b>Filters</b><button id="clear" class="link">Clear all</button></div><div class="filter-grid">';FILTER_FIELDS.forEach(c=>{let opts=uniq(c),sel=new Set(filters[c]||[]);h+=`<div class="filter"><label>${esc(c)}</label><div class="multi" data-col="${esc(c)}"><button class="multi-trigger" type="button"><span>${esc(triggerLabel(c))}</span><span>⌄</span></button><div class="multi-menu"><input class="multi-search" placeholder="Search…"><div class="multi-options">${opts.map(v=>`<label class="multi-option"><input type="checkbox" value="${esc(v)}" ${sel.has(v)?'checked':''}><span>${esc(v)}</span></label>`).join('')}</div><div class="multi-foot"><button class="link-btn select-all" type="button">Select all</button><button class="link-btn clear-one" type="button">Clear</button></div></div></div></div>`});h+=`<div class="filter"><label>Search SKU / EAN</label><input id="search" placeholder="Product or EAN…" value="${esc(filters.__q||'')}"></div></div></div>`;return h}
function renderFilters(){document.getElementById('filterBar').innerHTML=filterBar();document.querySelectorAll('.multi').forEach(m=>{let c=m.dataset.col;m.querySelector('.multi-trigger').onclick=e=>{e.stopPropagation();document.querySelectorAll('.multi.open').forEach(x=>x.classList.remove('open'));m.classList.add('open')};m.querySelector('.multi-menu').onclick=e=>e.stopPropagation();m.querySelector('.multi-search').oninput=e=>{let q=e.target.value.toLowerCase();m.querySelectorAll('.multi-option').forEach(x=>x.style.display=x.textContent.toLowerCase().includes(q)?'':'none')};m.querySelectorAll('.multi-option input').forEach(cb=>cb.onchange=()=>{let v=[...m.querySelectorAll('input:checked')].map(x=>x.value);v.length?filters[c]=v:delete filters[c];render()});m.querySelector('.select-all').onclick=()=>{filters[c]=uniq(c);render()};m.querySelector('.clear-one').onclick=()=>{delete filters[c];render()}});let q=document.getElementById('search');if(q)q.oninput=e=>{filters.__q=e.target.value;render()};document.getElementById('clear').onclick=()=>{filters={};render()}}
document.addEventListener('click',()=>document.querySelectorAll('.multi.open').forEach(x=>x.classList.remove('open')));
function getCols(id,baseCols){try{let x=JSON.parse(localStorage.getItem('cols_'+id)||'null');if(Array.isArray(x))return x.filter(c=>baseCols.includes(c)).concat(baseCols.filter(c=>!x.includes(c)))}catch(_){}return baseCols}
function columnChooser(id,cols){return `<div class="column-picker hidden" id="picker-${esc(id)}"><div class="column-picker-head"><b>Change column sequence</b><button class="picker-close" data-close-picker="${esc(id)}">✕</button></div><div id="picker-list-${esc(id)}">${cols.map((c,i)=>`<div class="column-item" data-col="${esc(c)}"><span>☷ ${esc(c)}</span><span><button data-up="${esc(c)}" ${i?'':'disabled'}>▲</button><button data-down="${esc(c)}" ${i<cols.length-1?'':'disabled'}>▼</button></span></div>`).join('')}</div><div class="column-picker-foot"><button class="btn small" data-save-cols="${esc(id)}">Apply</button><button class="btn secondary small" data-reset-cols="${esc(id)}">Reset</button></div></div>`}
function table(rows,cols,id,limit=1000){cols=getCols(id,cols);let shown=rows.slice(0,limit);return `<div class="table-tools"><div class="muted">${rows.length.toLocaleString('en-IN')} rows</div><button class="btn secondary small" data-open-picker="${esc(id)}">⚙ Columns</button>${columnChooser(id,cols)}</div><div class="table-wrap"><table data-sort-id="${esc(id)}"><thead><tr>${cols.map(c=>`<th>${esc(c)}</th>`).join('')}</tr></thead><tbody>${shown.map(r=>`<tr>${cols.map(c=>`<td class="${typeof r[c]==='number'?'num':''}">${cell(c,r[c])}</td>`).join('')}</tr>`).join('')}</tbody></table></div>${rows.length>limit?`<div class="table-foot">Showing first ${limit.toLocaleString('en-IN')} rows.</div>`:''}`}
document.addEventListener('click',e=>{let o=e.target.closest('[data-open-picker]');if(o){document.querySelectorAll('.column-picker').forEach(x=>x.classList.add('hidden'));document.getElementById('picker-'+o.dataset.openPicker)?.classList.remove('hidden')}let close=e.target.closest('[data-close-picker]');if(close)document.getElementById('picker-'+close.dataset.closePicker)?.classList.add('hidden');let up=e.target.closest('[data-up]'),down=e.target.closest('[data-down]');if(up||down){let p=(up||down).closest('.column-picker'),list=[...p.querySelectorAll('.column-item')],i=list.findIndex(x=>x.dataset.col===(up||down).dataset[up?'up':'down']);let j=up?i-1:i+1;if(i<0||j<0||j>=list.length)return;list[j].parentNode.insertBefore(list[i],up?list[j]:list[j].nextSibling);[...p.querySelectorAll('button[data-up],button[data-down]')].forEach(b=>{let items=[...p.querySelectorAll('.column-item')],ix=items.findIndex(x=>x.dataset.col===b.dataset.up||x.dataset.col===b.dataset.down);b.disabled=b.hasAttribute('data-up')?ix===0:ix===items.length-1})}let save=e.target.closest('[data-save-cols]');if(save){let p=document.getElementById('picker-'+save.dataset.saveCols),arr=[...p.querySelectorAll('.column-item')].map(x=>x.dataset.col);localStorage.setItem('cols_'+save.dataset.saveCols,JSON.stringify(arr));p.classList.add('hidden');render()}let reset=e.target.closest('[data-reset-cols]');if(reset){localStorage.removeItem('cols_'+reset.dataset.resetCols);render()}});
function chartFormat(v,mode){return mode==='value'?moneyL(v):qty(v)}
function paretoSummary(d){let m={};d.forEach(r=>{let p=normalizePareto(r.Pareto);m[p]??={stockQty:0,stockVal:0,l3mQty:0,l3mVal:0,cmQty:0,cmVal:0,lyQty:0,lyVal:0};let g=m[p];g.stockQty+=+r.Stock||0;g.stockVal+=+r['Total MRP Value']||0;g.l3mQty+=+r['L3M Avg Qty']||0;g.l3mVal+=+r['L3M Avg Value']||0;g.cmQty+=+r['Current Month Qty']||0;g.cmVal+=+r['Current Month Value']||0;g.lyQty+=+r['LY Qty']||0;g.lyVal+=+r['LY Value']||0});return m}
function overview(){let d=FILTERED,byType={};d.forEach(r=>{let t=r.Type||'Other';byType[t]??={ly:0,l3m:0,cm:0,lyVal:0,l3mVal:0,cmVal:0};byType[t].ly+=+r['LY Qty']||0;byType[t].l3m+=+r['L3M Avg Qty']||0;byType[t].cm+=+r['Current Month Qty']||0;byType[t].lyVal+=+r['LY Value']||0;byType[t].l3mVal+=+r['L3M Avg Value']||0;byType[t].cmVal+=+r['Current Month Value']||0});let types=['EBO','Kiosk','Airport'].filter(t=>byType[t]);let p=paretoSummary(d),cats=Object.keys(p);document.getElementById('app').innerHTML=`<div class="kpi-section"><div class="kpi-row-label">QUANTITY</div><div class="kpis kpis-4">${kpi('Stock Qty',qty(sum(d,'Stock')),'Current stock')}${kpi('LY Qty',qty(sum(d,'LY Qty')),'Last year')}${kpi('L3M Avg Qty',qty(sum(d,'L3M Avg Qty')),'Run rate')}${kpi('Current Month Qty',qty(sum(d,'Current Month Qty')),'Current month')}</div><div class="kpi-row-label">VALUE</div><div class="kpis kpis-4">${kpi('Stock Value',moneyL(sum(d,'Total MRP Value')),'Current MRP')}${kpi('LY Value',moneyL(sum(d,'LY Value')),'Last year')}${kpi('L3M Avg Value',moneyL(sum(d,'L3M Avg Value')),'Run rate')}${kpi('Current Month Value',moneyL(sum(d,'Current Month Value')),'Current month')}</div></div><div class="grid2"><div class="card"><div class="card-title">Pareto Contribution — ${paretoMode==='value'?'Value':'Qty'} <span class="seg"><button id="pQty" class="${paretoMode==='qty'?'active':''}">Qty</button><button id="pVal" class="${paretoMode==='value'?'active':''}">Value</button></span></div><div id="paretoChart" class="chart"></div></div><div class="card"><div class="card-title">Stock vs L3M Average — ${paretoMode==='value'?'Value':'Qty'}</div><div id="stockSalesChart" class="chart"></div></div></div><div class="grid2"><div class="card"><div class="card-title">Type-wise Sales — Quantity</div><div id="typeQtyChart" class="chart"></div></div><div class="card"><div class="card-title">Type-wise Sales — Value</div><div id="typeValueChart" class="chart"></div></div></div>`;document.getElementById('pQty').onclick=()=>{paretoMode='qty';overview()};document.getElementById('pVal').onclick=()=>{paretoMode='value';overview()};let total=p?Object.values(p).reduce((a,x)=>a+(paretoMode==='value'?x.stockVal:x.stockQty),0):0;chart('paretoChart',{...base,tooltip:{trigger:'item',formatter:q=>{let g=p[q.name]||{};let lines=[`<b>${esc(q.name)}</b>`,`Contribution: ${pct1(q.percent)}`];if(paretoMode==='value')lines.push(`Stock Value: ${moneyL(g.stockVal)}`,`L3M Avg Value: ${moneyL(g.l3mVal)}`,`Current Month Value: ${moneyL(g.cmVal)}`,`LY Value: ${moneyL(g.lyVal)}`);else lines.push(`Stock Qty: ${qty(g.stockQty)}`,`L3M Avg Qty: ${qty(g.l3mQty)}`,`Current Month Qty: ${qty(g.cmQty)}`,`LY Qty: ${qty(g.lyQty)}`);return lines.join('<br>')},},series:[{type:'pie',radius:['42%','68%'],data:cats.map(k=>({name:k,value:paretoMode==='value'?p[k].stockVal:p[k].stockQty})),label:{show:true,formatter:q=>`${q.name}\n${paretoMode==='value'?moneyL(q.value):qty(q.value)} (${pct1(q.percent)})`},labelLine:{show:true}}]});let stockSales= cats.map(k=>p[k]);chart('stockSalesChart',{...base,legend:{show:true,top:5},xAxis:{type:'category',data:cats},yAxis:{type:'value',axisLabel:{formatter:v=>chartFormat(v,paretoMode)}},series:[{name:paretoMode==='value'?'Stock Value':'Stock Qty',type:'bar',data:stockSales.map(x=>paretoMode==='value'?x.stockVal:x.stockQty),barMaxWidth:34,label:{show:true,position:'top',formatter:q=>chartFormat(q.value,paretoMode)}},{name:paretoMode==='value'?'L3M Avg Value':'L3M Avg Qty',type:'bar',data:stockSales.map(x=>paretoMode==='value'?x.l3mVal:x.l3mQty),barMaxWidth:34,label:{show:true,position:'top',formatter:q=>chartFormat(q.value,paretoMode)}}]});chart('typeQtyChart',{...base,legend:{show:true,top:5},xAxis:{type:'category',data:types},yAxis:{type:'value',axisLabel:{formatter:qty}},series:[{name:'LY Qty',type:'bar',data:types.map(t=>byType[t].ly),label:{show:true,position:'top',formatter:q=>qty(q.value)}},{name:'L3M Avg Qty',type:'bar',data:types.map(t=>byType[t].l3m),label:{show:true,position:'top',formatter:q=>qty(q.value)}},{name:'CM Qty',type:'bar',data:types.map(t=>byType[t].cm),label:{show:true,position:'top',formatter:q=>qty(q.value)}}]});chart('typeValueChart',{...base,legend:{show:true,top:5},xAxis:{type:'category',data:types},yAxis:{type:'value',axisLabel:{formatter:moneyL}},series:[{name:'LY Value',type:'bar',data:types.map(t=>byType[t].lyVal),label:{show:true,position:'top',formatter:q=>moneyL(q.value)}},{name:'L3M Avg Value',type:'bar',data:types.map(t=>byType[t].l3mVal),label:{show:true,position:'top',formatter:q=>moneyL(q.value)}},{name:'CM Value',type:'bar',data:types.map(t=>byType[t].cmVal),label:{show:true,position:'top',formatter:q=>moneyL(q.value)}}]})}
function nodCounts(rows){return {total:rows.length,lt15:rows.filter(r=>+r.NOD<15).length,n15_30:rows.filter(r=>+r.NOD>=15&&+r.NOD<=30).length,n31_60:rows.filter(r=>+r.NOD>30&&+r.NOD<=60).length,gt60:rows.filter(r=>+r.NOD>60).length}}
function aggregateSku(rows){let m={};rows.forEach(r=>{let k=r['Product Name']||r['EAN Code'];m[k]??={};let g=m[k];DATA_COLUMNS.forEach(c=>{if(['Stock','Total MRP Value','L3M Avg Qty','L3M Avg Value','LY Qty','LY Value','Current Month Qty','Current Month Value'].includes(c))g[c]=(g[c]||0)+(+r[c]||0);else if(g[c]==null||g[c]==='')g[c]=r[c]});g['Product Name']=k});return Object.values(m).map(g=>({...g,NOD:g['L3M Avg Qty']>0?g.Stock*31/g['L3M Avg Qty']:0}));}
function aggregateStore(rows){let m={};rows.forEach(r=>{let k=r['Store Name']||'Unknown';m[k]??={};let g=m[k];DATA_COLUMNS.forEach(c=>{if(['Stock','Total MRP Value','L3M Avg Qty','L3M Avg Value','LY Qty','LY Value','Current Month Qty','Current Month Value'].includes(c))g[c]=(g[c]||0)+(+r[c]||0);else if(g[c]==null||g[c]==='')g[c]=r[c]});g['Store Name']=k});return Object.values(m).map(g=>({...g,NOD:g['L3M Avg Qty']>0?g.Stock*31/g['L3M Avg Qty']:0}));}
function comboSkuOption(id,arr,title){if(!arr.length){document.getElementById(id).innerHTML='<div class="empty">No SKUs available.</div>';return}let a=arr.slice(0,5);chart(id,{...base,tooltip:{trigger:'axis',axisPointer:{type:'shadow'},formatter:ps=>{let i=ps[0]?.dataIndex||0,x=a[i];return `<b>${esc(x['Product Name'])}</b><br>Stock: ${qty(x.Stock)}<br>L3M Avg: ${qty(x['L3M Avg Qty'])}<br>CM: ${qty(x['Current Month Qty'])}<br>LY: ${qty(x['LY Qty'])}`}},legend:{show:true,top:5},xAxis:{type:'category',data:a.map(x=>x['Product Name']),axisLabel:{rotate:28,fontSize:9,interval:0,formatter:v=>String(v).length>18?String(v).slice(0,18)+'…':v}},yAxis:{type:'value',axisLabel:{formatter:qty}},series:[{name:'Stock Qty',type:'bar',data:a.map(x=>x.Stock),barMaxWidth:32,label:{show:true,position:'top',formatter:q=>qty(q.value)}},{name:'L3M Avg',type:'line',data:a.map(x=>x['L3M Avg Qty']),symbol:'circle',symbolSize:7},{name:'CM',type:'line',data:a.map(x=>x['Current Month Qty']),symbol:'circle',symbolSize:7},{name:'LY',type:'line',data:a.map(x=>x['LY Qty']),symbol:'circle',symbolSize:7}]},()=>openChartModal(title,arr));}
function openChartModal(title,arr){let ov=document.getElementById('modalOverlay');document.getElementById('modalTitle').textContent=title+' — Full View';document.getElementById('modalSub').textContent=`${arr.length} SKUs • click/hover for exact values`;document.getElementById('modalBody').innerHTML=`<div class="modal-tools"><span class="muted">All SKUs shown below</span><span class="seg"><button id="mcQty" class="active">Qty</button><button id="mcVal">Value</button></span></div><div id="modalChart" class="modal-chart"></div>`;let mode='qty';function draw(){let a=arr;chart('modalChart',{...base,grid:{left:55,right:45,top:70,bottom:95,containLabel:true},tooltip:{trigger:'axis',formatter:ps=>{let i=ps[0]?.dataIndex||0,x=a[i];return `<b>${esc(x['Product Name'])}</b><br>Stock: ${mode==='qty'?qty(x.Stock):moneyL(x['Total MRP Value'])}<br>L3M Avg: ${mode==='qty'?qty(x['L3M Avg Qty']):moneyL(x['L3M Avg Value'])}<br>CM: ${mode==='qty'?qty(x['Current Month Qty']):moneyL(x['Current Month Value'])}<br>LY: ${mode==='qty'?qty(x['LY Qty']):moneyL(x['LY Value'])}`}},legend:{show:true,top:5},dataZoom:[{type:'inside'},{type:'slider',bottom:12,height:16}],xAxis:{type:'category',data:a.map(x=>x['Product Name']),axisLabel:{rotate:35,interval:0,formatter:v=>String(v).length>22?String(v).slice(0,22)+'…':v}},yAxis:{type:'value',axisLabel:{formatter:v=>mode==='qty'?qty(v):moneyL(v)}},series:[{name:mode==='qty'?'Stock Qty':'Stock Value',type:'bar',data:a.map(x=>mode==='qty'?x.Stock:x['Total MRP Value']),barMaxWidth:34,label:{show:true,position:'top',formatter:q=>mode==='qty'?qty(q.value):moneyL(q.value)}},{name:mode==='qty'?'L3M Avg Qty':'L3M Avg Value',type:'line',data:a.map(x=>mode==='qty'?x['L3M Avg Qty']:x['L3M Avg Value']),symbol:'circle',symbolSize:7},{name:mode==='qty'?'CM Qty':'CM Value',type:'line',data:a.map(x=>mode==='qty'?x['Current Month Qty']:x['Current Month Value']),symbol:'circle',symbolSize:7},{name:mode==='qty'?'LY Qty':'LY Value',type:'line',data:a.map(x=>mode==='qty'?x['LY Qty']:x['LY Value']),symbol:'circle',symbolSize:7}]});}document.getElementById('mcQty').onclick=()=>{mode='qty';document.getElementById('mcQty').classList.add('active');document.getElementById('mcVal').classList.remove('active');draw()};document.getElementById('mcVal').onclick=()=>{mode='value';document.getElementById('mcVal').classList.add('active');document.getElementById('mcQty').classList.remove('active');draw()};draw();ov.classList.add('open')}
function products(){let rows=aggregateSku(FILTERED);let top10=rows.filter(x=>normalizePareto(x.Pareto)==='Top 10').sort((a,b)=>b.Stock-a.Stock),top25=rows.filter(x=>normalizePareto(x.Pareto)==='Top 25').sort((a,b)=>b.Stock-a.Stock),nc=nodCounts(rows);let momentum=rows.slice().sort((a,b)=>Math.abs(b['Current Month Qty']-b['LY Qty'])-Math.abs(a['Current Month Qty']-a['LY Qty'])).slice(0,12);document.getElementById('app').innerHTML=`<div class="kpis kpis-4">${kpi('Total Unique SKUs',qty(rows.length),'Filtered SKUs')}${kpi('Stock Qty',qty(sum(FILTERED,'Stock')),'Current stock')}${kpi('Stock Value',moneyL(sum(FILTERED,'Total MRP Value')),'Current MRP')}${kpi('NOD Count',qty(nc.total),'Total SKU NOD')}</div><div class="kpis kpis-4">${kpi('NOD <15',qty(nc.lt15),'Critical','bad')}${kpi('NOD 15–30',qty(nc.n15_30),'Watch')}${kpi('NOD 31–60',qty(nc.n31_60),'Healthy')}${kpi('NOD >60',qty(nc.gt60),'Overstock','bad')}</div><div class="grid2"><div class="card clickable-card"><div class="card-title">Top 10 Pareto SKUs — Top 5 Preview <span class="muted">Click chart for all 10</span></div><div id="top10Chart" class="chart"></div></div><div class="card clickable-card"><div class="card-title">Top 25 Pareto SKUs — Top 5 Preview <span class="muted">Click chart for all 25</span></div><div id="top25Chart" class="chart"></div></div></div><div class="card"><div class="card-title">SKU Sales Momentum</div><div class="muted">LY → L3M Average → Current Month. Highest movement-change SKUs shown.</div><div id="momentumChart" class="chart"></div></div><div class="card"><div class="card-title">SKU Explorer</div>${table(rows,DATA_COLUMNS.filter(c=>rows.some(r=>Object.prototype.hasOwnProperty.call(r,c))),'products',1000)}</div>`;comboSkuOption('top10Chart',top10,'Top 10 Pareto SKUs');comboSkuOption('top25Chart',top25,'Top 25 Pareto SKUs');if(momentum.length)chart('momentumChart',{...base,legend:{show:true,top:5},dataZoom:[{type:'inside'},{type:'slider',bottom:8,height:16}],xAxis:{type:'category',data:momentum.map(x=>x['Product Name']),axisLabel:{rotate:30,fontSize:9,interval:0,formatter:v=>String(v).length>18?String(v).slice(0,18)+'…':v}},yAxis:{type:'value',axisLabel:{formatter:qty}},series:[{name:'LY',type:'bar',data:momentum.map(x=>x['LY Qty'])},{name:'L3M Avg',type:'bar',data:momentum.map(x=>x['L3M Avg Qty'])},{name:'CM',type:'bar',data:momentum.map(x=>x['Current Month Qty'])}]});document.querySelectorAll('table[data-sort-id="products"] tbody tr').forEach(tr=>tr.onclick=()=>{let name=tr.querySelector('td')?.textContent;let detail=FILTERED.filter(x=>(x['Product Name']||x['EAN Code'])===name);openDetailModal(name,'Shop detail',detail,'sku')})}
function stores(){let rows=aggregateStore(FILTERED),skuRows=aggregateSku(FILTERED),nc=nodCounts(skuRows),top=rows.slice().sort((a,b)=>b.Stock-a.Stock).slice(0,15),mom=rows.slice().sort((a,b)=>Math.abs(b['Current Month Qty']-b['LY Qty'])-Math.abs(a['Current Month Qty']-a['LY Qty'])).slice(0,12),types=['EBO','Kiosk','Airport'].filter(t=>FILTERED.some(r=>r.Type===t));document.getElementById('app').innerHTML=`<div class="kpis kpis-4">${kpi('Total Stores',qty(rows.length),'Filtered stores')}${kpi('Stock Qty',qty(sum(FILTERED,'Stock')),'Current stock')}${kpi('Stock Value',moneyL(sum(FILTERED,'Total MRP Value')),'Current MRP')}${kpi('L3M Avg Qty',qty(sum(FILTERED,'L3M Avg Qty')),'Run rate')}</div><div class="kpis kpis-4">${kpi('Current Month Qty',qty(sum(FILTERED,'Current Month Qty')),'Current month')}${kpi('LY Qty',qty(sum(FILTERED,'LY Qty')),'Last year')}${kpi('NOD <15',qty(nc.lt15),'SKU count','bad')}${kpi('NOD 15–30',qty(nc.n15_30),'SKU count')}</div><div class="kpis kpis-4">${kpi('NOD 31–60',qty(nc.n31_60),'SKU count')}${kpi('NOD >60',qty(nc.gt60),'SKU count','bad')}${kpi('NOD Count',qty(nc.total),'Total SKU count')}${kpi('CM vs L3M',pct1(sum(FILTERED,'L3M Avg Qty')?sum(FILTERED,'Current Month Qty')/sum(FILTERED,'L3M Avg Qty')*100-100:0),'Sales movement')}</div><div class="grid2"><div class="card"><div class="card-title">Store Stock vs Sales Run Rate — Top 15</div><div id="storePerfChart" class="chart chart-tall"></div></div><div class="card"><div class="card-title">Store Sales Momentum — Top 12</div><div id="storeMomentumChart" class="chart chart-tall"></div></div></div><div class="grid2"><div class="card"><div class="card-title">Store NOD Distribution</div><div id="storeNodChart" class="chart"></div></div><div class="card"><div class="card-title">Type-wise Store Performance</div><div id="storeTypeChart" class="chart"></div></div></div><div class="card"><div class="card-title">Store Analysis</div>${table(rows,DATA_COLUMNS.filter(c=>rows.some(r=>Object.prototype.hasOwnProperty.call(r,c))),'stores',1000)}</div>`;let topR=top.slice().reverse();chart('storePerfChart',{...base,legend:{show:true,top:5},dataZoom:[{type:'inside'},{type:'slider',bottom:8,height:16}],xAxis:{type:'value',axisLabel:{formatter:qty}},yAxis:{type:'category',data:topR.map(x=>x['Store Name']),axisLabel:{fontSize:9}},series:[{name:'Stock',type:'bar',data:topR.map(x=>x.Stock),label:{show:true,position:'right',formatter:q=>qty(q.value)}},{name:'L3M Avg',type:'bar',data:topR.map(x=>x['L3M Avg Qty']),label:{show:true,position:'right',formatter:q=>qty(q.value)}},{name:'CM',type:'bar',data:topR.map(x=>x['Current Month Qty']),label:{show:true,position:'right',formatter:q=>qty(q.value)}}]});chart('storeMomentumChart',{...base,legend:{show:true,top:5},dataZoom:[{type:'inside'},{type:'slider',bottom:8,height:16}],xAxis:{type:'category',data:mom.map(x=>x['Store Name']),axisLabel:{rotate:28,fontSize:9,interval:0,formatter:v=>String(v).length>18?String(v).slice(0,18)+'…':v}},yAxis:{type:'value',axisLabel:{formatter:qty}},series:[{name:'LY',type:'line',data:mom.map(x=>x['LY Qty']),symbol:'circle',symbolSize:7},{name:'L3M Avg',type:'line',data:mom.map(x=>x['L3M Avg Qty']),symbol:'circle',symbolSize:7},{name:'CM',type:'line',data:mom.map(x=>x['Current Month Qty']),symbol:'circle',symbolSize:7}]});let buckets={'NOD <15':nc.lt15,'NOD 15–30':nc.n15_30,'NOD 31–60':nc.n31_60,'NOD >60':nc.gt60};chart('storeNodChart',{...base,xAxis:{type:'category',data:Object.keys(buckets)},yAxis:{type:'value',axisLabel:{formatter:qty}},series:[{name:'SKUs',type:'bar',data:Object.values(buckets),barMaxWidth:55,label:{show:true,position:'top',formatter:q=>qty(q.value)}}]});let tm={};FILTERED.forEach(r=>{let t=r.Type||'Other';tm[t]??={stock:0,l3m:0,cm:0,ly:0};tm[t].stock+=+r.Stock||0;tm[t].l3m+=+r['L3M Avg Qty']||0;tm[t].cm+=+r['Current Month Qty']||0;tm[t].ly+=+r['LY Qty']||0});chart('storeTypeChart',{...base,legend:{show:true,top:5},xAxis:{type:'category',data:types},yAxis:{type:'value',axisLabel:{formatter:qty}},series:[{name:'Stock',type:'bar',data:types.map(t=>tm[t].stock),label:{show:true,position:'top',formatter:q=>qty(q.value)}},{name:'L3M Avg',type:'line',data:types.map(t=>tm[t].l3m),symbol:'circle',symbolSize:7},{name:'CM',type:'line',data:types.map(t=>tm[t].cm),symbol:'circle',symbolSize:7},{name:'LY',type:'line',data:types.map(t=>tm[t].ly),symbol:'circle',symbolSize:7}]});document.querySelectorAll('table[data-sort-id="stores"] tbody tr').forEach(tr=>tr.onclick=()=>{let store=tr.querySelector('td')?.textContent;let detail=FILTERED.filter(r=>r['Store Name']===store);openDetailModal(store,'SKU detail',detail,'store')})}
function dataTable(){document.getElementById('app').innerHTML=`<div class="card"><div class="card-title">Complete Data Table</div>${table(FILTERED,DATA_COLUMNS,'datatable',5000)}</div>`}
function openDetailModal(title,sub,rows,mode){let ov=document.getElementById('modalOverlay');document.getElementById('modalTitle').textContent=title;document.getElementById('modalSub').textContent=sub;document.getElementById('modalBody').innerHTML=`<div class="modal-filters"><input id="modalSearch" placeholder="Search SKU / EAN / Product / Shop…"><select id="modalFilter"><option value="">All ${mode==='sku'?'Shops':'SKUs'}</option></select></div><div id="modalTable"></div>`;let opts=mode==='sku'?[...new Set(rows.map(r=>r['Store Name']))]:[...new Set(rows.map(r=>r['Product Name']))];document.getElementById('modalFilter').innerHTML+=opts.sort().map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('');let cols=DATA_COLUMNS.filter(c=>rows.some(r=>Object.prototype.hasOwnProperty.call(r,c)));function draw(){let q=(document.getElementById('modalSearch').value||'').toLowerCase(),f=document.getElementById('modalFilter').value;let rr=rows.filter(r=>!q||String(r['Product Name']).toLowerCase().includes(q)||String(r['EAN Code']).toLowerCase().includes(q)||String(r['Store Name']).toLowerCase().includes(q)).filter(r=>!f||(mode==='sku'?r['Store Name']===f:r['Product Name']===f));document.getElementById('modalTable').innerHTML=table(rr,cols,'modal',2000)}document.getElementById('modalSearch').oninput=draw;document.getElementById('modalFilter').onchange=draw;draw();ov.classList.add('open')}
async function loadStock(force=false){let q=new URLSearchParams({_ts:Date.now()});if(force)q.set('refresh','1');let r=await fetch('/api/data?'+q,{cache:'no-store'}),j=await r.json();if(!j.ok)throw Error(j.error);DATA_COLUMNS=j.source_columns||j.columns||[];DATA=j.records.map(r=>({...r,Pareto:normalizePareto(r.Pareto),NOD:Number(r.NOD||0),'NOD Bucket':nodBucket(r.NOD)}));document.getElementById('sourceBadge').textContent=`${j.source} • ${j.rows.toLocaleString('en-IN')} rows`;renderFilters();render()}
async function ensureVarianceLoaded(force=false){if(force||!VAR.length){let r=await fetch('/api/variance?_ts='+Date.now()+(force?'&refresh=1':''),{cache:'no-store'}),j=await r.json();if(!j.ok)throw Error(j.error);VAR=j.records||[]}render()}
function variance(){let rows=VAR.slice();if(varianceShop)rows=rows.filter(r=>r['Store Name']===varianceShop);if(varianceSkuSearch){let q=varianceSkuSearch.toLowerCase();rows=rows.filter(r=>String(r['EAN Code']).toLowerCase().includes(q)||String(r['Product Name']).toLowerCase().includes(q))}let allIssues=rows.filter(r=>!r['Movement Check']||(r['Live Submission']&&Math.abs(+r['Difference Qty']||0)>0));if(varianceIssueOnly)rows=allIssues;let metric=varianceMetric==='qty'?'Difference Qty':'Difference Value',abs=rows.reduce((a,r)=>a+Math.abs(+r[metric]||0),0),shops=[...new Set(VAR.map(r=>r['Store Name']).filter(Boolean))].sort(),live=rows.filter(r=>r['Live Submission']),liveQty=sum(live,'Actual Closing Qty'),liveVal=sum(live,'Actual Closing Value');document.getElementById('app').innerHTML=`<div class="variance-filters"><span class="seg"><button id="vQty" class="${varianceMetric==='qty'?'active':''}">Qty</button><button id="vVal" class="${varianceMetric==='value'?'active':''}">Value</button></span><select id="vShop"><option value="">All Shops</option>${shops.map(s=>`<option ${s===varianceShop?'selected':''} value="${esc(s)}">${esc(s)}</option>`).join('')}</select><input id="vSku" placeholder="Search SKU / EAN / Product" value="${esc(varianceSkuSearch)}"><label class="check"><input id="issueOnly" type="checkbox" ${varianceIssueOnly?'checked':''}> Issues only</label><button id="vCsv" class="btn secondary small">Download CSV</button></div><div class="kpis kpis-5">${kpi('Opening Stock',varianceMetric==='qty'?qty(sum(rows,'Opening Stock Qty')):moneyL(sum(rows,'Opening Stock Value')),'Opening')}${kpi('Inward Receipts',varianceMetric==='qty'?qty(sum(rows,'Inward Qty')):moneyL(sum(rows,'Inward Value')),'Inward')}${kpi('Tertiary',varianceMetric==='qty'?qty(sum(rows,'Tertiary Qty')):moneyL(sum(rows,'Tertiary Value')),'Dispatch')}${kpi('System Closing',varianceMetric==='qty'?qty(sum(rows,'System Stock Qty')):moneyL(sum(rows,'System Stock Qty')),'Current Stock_Data')}${kpi('Field Actual',varianceMetric==='qty'?qty(liveQty):moneyL(liveVal),'Latest submitted total')}</div><div class="kpis kpis-4">${kpi('Reconciled Rows',qty(rows.filter(r=>r['Movement Check']).length),'Op + Inward − Tertiary = Closing',rows.filter(r=>r['Movement Check']).length?'good':'')}${kpi('Error Rows',qty(rows.filter(r=>!r['Movement Check']).length),'Movement errors',rows.filter(r=>!r['Movement Check']).length?'bad':'good')}${kpi('Physical Variance',varianceMetric==='qty'?qty(abs):moneyL(abs),'Actual vs system stock')}${kpi('Issue Rate',pct1(rows.length?allIssues.length/rows.length*100:0),'Movement or physical issue',allIssues.length?'bad':'good')}</div><div class="grid2"><div class="card"><div class="card-title">Movement Reconciliation</div><div id="movementChart" class="chart"></div></div><div class="card"><div class="card-title">Physical Variance by Shop</div><div id="varianceShopChart" class="chart chart-tall"></div></div></div><div class="grid2"><div class="card"><div class="card-title">Top Variance SKUs</div><div id="varianceSkuChart" class="chart chart-tall"></div></div><div class="card"><div class="card-title">Variance Direction</div><div id="varianceDirectionChart" class="chart"></div></div></div><div class="card"><div class="card-title">Variance Detail</div>${table(rows,Object.keys(rows[0]||{}).filter(c=>c!=='__proto__'),'variance',5000)}</div>`;document.getElementById('vQty').onclick=()=>{varianceMetric='qty';render()};document.getElementById('vVal').onclick=()=>{varianceMetric='value';render()};document.getElementById('vShop').onchange=e=>{varianceShop=e.target.value;render()};document.getElementById('vSku').oninput=e=>{varianceSkuSearch=e.target.value;render()};document.getElementById('issueOnly').onchange=e=>{varianceIssueOnly=e.target.checked;render()};document.getElementById('vCsv').onclick=()=>location.href='/api/variance-export?store='+encodeURIComponent(varianceShop)+'&sku='+encodeURIComponent(varianceSkuSearch)+'&issues='+(varianceIssueOnly?'1':'0');let mv={Opening:0,Inward:0,Tertiary:0,Closing:0};rows.forEach(r=>{mv.Opening+=+r[varianceMetric==='qty'?'Opening Stock Qty':'Opening Stock Value']||0;mv.Inward+=+r[varianceMetric==='qty'?'Inward Qty':'Inward Value']||0;mv.Tertiary+=+r[varianceMetric==='qty'?'Tertiary Qty':'Tertiary Value']||0;mv.Closing+=+r[varianceMetric==='qty'?'Closing Stock Qty':'Closing Stock Value']||0});chart('movementChart',{...base,xAxis:{type:'category',data:Object.keys(mv)},yAxis:{type:'value',axisLabel:{formatter:varianceMetric==='qty'?qty:moneyL}},series:[{name:varianceMetric==='qty'?'Qty':'Value',type:'bar',data:Object.values(mv),barMaxWidth:55,label:{show:true,position:'top',formatter:q=>varianceMetric==='qty'?qty(q.value):moneyL(q.value)}}]});let sm={};allIssues.filter(r=>r['Live Submission']).forEach(r=>{sm[r['Store Name']]=(sm[r['Store Name']]||0)+Math.abs(+r[metric]||0)});let se=Object.entries(sm).sort((a,b)=>b[1]-a[1]).slice(0,10).reverse();if(se.length)chart('varianceShopChart',{...base,xAxis:{type:'value',axisLabel:{formatter:varianceMetric==='qty'?qty:moneyL}},yAxis:{type:'category',data:se.map(x=>x[0]),axisLabel:{fontSize:9}},series:[{name:'Variance',type:'bar',data:se.map(x=>x[1]),label:{show:true,position:'right',formatter:q=>varianceMetric==='qty'?qty(q.value):moneyL(q.value)}}]});else document.getElementById('varianceShopChart').innerHTML='<div class="empty">No physical variance submitted yet.</div>';let vs=allIssues.filter(r=>r['Live Submission']).map(r=>({n:r['Product Name'],v:Math.abs(+r[metric]||0)})).sort((a,b)=>b.v-a.v).slice(0,10).reverse();if(vs.length)chart('varianceSkuChart',{...base,xAxis:{type:'value',axisLabel:{formatter:varianceMetric==='qty'?qty:moneyL}},yAxis:{type:'category',data:vs.map(x=>x.n),axisLabel:{fontSize:9}},series:[{name:'Variance',type:'bar',data:vs.map(x=>x.v),label:{show:true,position:'right',formatter:q=>varianceMetric==='qty'?qty(q.value):moneyL(q.value)}}]});else document.getElementById('varianceSkuChart').innerHTML='<div class="empty">No physical variance submitted yet.</div>';let shortage=allIssues.filter(r=>r['Live Submission']&&+r['Difference Qty']<0).length,excess=allIssues.filter(r=>r['Live Submission']&&+r['Difference Qty']>0).length;chart('varianceDirectionChart',{...base,xAxis:{type:'category',data:['Shortage','Excess']},yAxis:{type:'value',axisLabel:{formatter:qty}},series:[{name:'SKUs',type:'bar',data:[shortage,excess],label:{show:true,position:'top',formatter:q=>qty(q.value)}}]})}
function render(){apply();charts.forEach(c=>c.dispose());charts=[];document.getElementById('pageTitle').textContent={overview:'Overview',products:'SKU Explorer',stores:'Store Analysis',table:'Data Table',variance:'Variance Analysis'}[page];document.getElementById('filterBar').style.display=page==='variance'?'none':'';if(page==='overview')overview();else if(page==='products')products();else if(page==='stores')stores();else if(page==='table')dataTable();else variance()}
async function load(){try{await loadStock(false)}catch(e){document.getElementById('app').innerHTML=`<div class="error"><b>Data load failed</b><br>${esc(e.message)}</div>`}}
document.querySelectorAll('.nav').forEach(b=>b.onclick=async()=>{document.querySelectorAll('.nav').forEach(x=>x.classList.remove('active'));b.classList.add('active');page=b.dataset.page;if(page==='variance'){document.getElementById('filterBar').style.display='none';try{await ensureVarianceLoaded(false)}catch(e){document.getElementById('app').innerHTML=`<div class="error">${esc(e.message)}</div>`}}else render()});
document.getElementById('refresh').onclick=async()=>{let b=document.getElementById('refresh');b.disabled=true;b.textContent='↻ Refreshing…';try{if(page==='variance'){VAR=[];await ensureVarianceLoaded(true)}else await loadStock(true)}catch(e){document.getElementById('app').innerHTML=`<div class="error"><b>Refresh failed</b><br>${esc(e.message)}</div>`}finally{b.disabled=false;b.textContent='↻ Refresh'}};
document.getElementById('modalClose').onclick=()=>document.getElementById('modalOverlay').classList.remove('open');document.getElementById('modalOverlay').onclick=e=>{if(e.target.id==='modalOverlay')e.currentTarget.classList.remove('open')};load();window.onresize=()=>charts.forEach(c=>c.resize());
